ポルタ
<div class="container">
    <div class="row">
        <div class="border rounded"></div>
        <% @portfolios.each do |portfolio|%>
          <div class="col-12"><%= link_to portfolio_path(portfolio) do %><%= portfolio.title %><% end %></div>
        <% end %>
    </div>
</div>